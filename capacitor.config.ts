import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simplecode.mobility',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
